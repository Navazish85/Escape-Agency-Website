import ContactUs from '@/components/contact-us';
import Layout from '@/layout';

const ContactUsPage = () => {
  return (
    <Layout>
      <ContactUs />
    </Layout>
  );
};

export default ContactUsPage;
